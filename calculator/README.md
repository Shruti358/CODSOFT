# Basic Calculator (HTML/CSS/JS)

## How to run
- Open `index.html` in your browser.

## Features
- Buttons for **+ − × ÷**
- **C** (clear), **DEL** (backspace), **%**, **±**, **.**, **=**
- Keyboard support: `0-9`, `+ - * /`, `Enter` (=), `Backspace` (DEL), `Esc` (C)

